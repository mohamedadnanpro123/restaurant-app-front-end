version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20  # ← Add this to use Node 20
    commands:
      - echo "📦 Installing dependencies..."
      - npm install  # Use npm install instead of npm ci
  
  build:
    commands:
      - echo "🏗️ Building React app..."
      - npm run build
  
  post_build:
    commands:
      - echo "✅ Build successful!"
      - ls -la build/

artifacts:
  files:
    - '**/*'
  base-directory: build
  name: frontend-build

cache:
  paths:
    - 'node_modules/**/*'
